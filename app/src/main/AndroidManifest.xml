<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.lifehealthsolutions.myhealthjournal">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controllers.ReminderActivity"></activity>
        <activity android:name=".controllers.AboutActivity" />
        <activity android:name=".controllers.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controllers.ConditionActivity" />
        <activity android:name=".controllers.CreateConditonActiv" />
        <activity android:name=".controllers.StartupSetup" />
        <activity android:name=".controllers.SignUpActivity" />
        <activity android:name=".controllers.SignInActivity" />
        <activity android:name=".controllers.EntryDataActiv" />
        <activity android:name=".controllers.CreateEntryActiv" />
        <activity android:name=".controllers.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>